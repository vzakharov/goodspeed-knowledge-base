Proposed squash title/body:

```
chore: stage always-loaded edits, trim CLAUDE.md, per-person costs (pr #11)
```

```
The agent loop vendored from vzakharov/muthur moved on, and three of
its changes apply here.

Editing CLAUDE.md, or any file rendered into every request's prefix,
invalidated the prompt cache of every session on the branch. Such
files are now edited through byte-identical copies under
.claude/staged/ (scripts/staged.sh, .claude/rules/staging.md), and
/finalize swaps them in once, merging whatever the real file gained
meanwhile; the vet run checks the copies.

CLAUDE.md is held to one test: a line stays only when it must hold on
turns where the process it concerns is not running. The vet roster's
rationale moves to a path-scoped .claude/rules/stack.md, skill
authoring to .claude/rules/skills.md, and procedure into the skills
that run it. check-skill-catalog.sh now also fails a CLAUDE.md section
citation naming no heading.

The cost ledger records whose session each row is (the operator
handle the SessionStart hook resolved, every earlier row filed under
@vzakharov) and sums spend by operator. Its Stop hook warns when the
transcript had not caught up with the turn, rather than the rule
assuming the last turn is lost; cost commits state the turn's spend;
the naming hook asks from the first prompt; and no procedure carries
another repo's rows. /finalize no longer claims the merge deploys.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
