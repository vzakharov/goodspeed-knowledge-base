Proposed squash title/body:

```
feat: chat's conversation list is a sidebar at the screen's edge (pr #10)
```

```
Chat squeezed the thread into eight twelfths of the reading width, with
the conversation list taking the rest inside the page. The list now
sits in a sidebar at the screen's left edge, so the thread gets the
same 896px measure as every other page.

From `lg` (1024px) up, the list is a fixed, independently scrolling
sidebar under the header, and the page reserves its width. Below `lg`
the old narrow layout holds: the list sits above a new conversation and
gives way to an open one. The breakpoint is `lg` rather than `sm`
because a 280px sidebar would squeeze the thread to ~300-550px at
640-1023px.

The breakpoint lives only in the chat page's stylesheet. Both lists
toggle there on `min-width`, so no width shows both or neither.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
