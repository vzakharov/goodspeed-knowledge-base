Proposed squash title/body:

```
docs: docs-only PRs finalize without the vet run (pr #16)
```

```
The vet run needs minutes and the local Supabase stack, and a diff that
touches only Markdown gets nothing from its build, type check or
database suites.

So /finalize takes its no-vet mode unasked when every changed file is
Markdown, the session cost rows aside, and keeps the two checks that
read it: prettier over the changed files, and the skill-catalog check
when .claude/ is touched. The attestation names both. The rule lives in
CLAUDE.md's Vetting section.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
