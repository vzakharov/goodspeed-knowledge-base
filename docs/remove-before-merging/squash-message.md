Proposed squash title/body:

```
docs: README edits from the voice review (pr #14)
```

```
A voice review of the README marked four places where it claimed more
than the code backs, or left a reviewer's obvious question unanswered.

The chunker's 4 characters a token and the retrieval's 0.25 cosine
threshold are both stated as proof-of-concept values; the threshold,
probably a low one, is to be measured on a synthetic dataset (#8), and
"What I would do with more time" carries that as its own item.

The AI layer section says that every provider the brief lists speaks
the OpenAI API, so one implementation per capability was enough, and
sketches how an Anthropic ChatModel would be added. The static export
is presented as one workable choice, with what a Next server would buy
and why a test assignment doesn't need it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
