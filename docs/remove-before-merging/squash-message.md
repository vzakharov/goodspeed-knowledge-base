Proposed squash title/body:

```
feat: #5 first run: / by document count, hover-only nav, key prompts (pr #9)
```

```
A reviewer's first run met a model overview at /, header links
underlined at rest, and a bootstrap that ended by telling them to open
apps/api/.env for the one value it could not fill in. This is the
first-run slice of the review of #1.

/ now redirects: to the documents page while a reader has none, to the
chat once they have some. It redirects rather than rendering either page
because both build their links on their own path. The model overview is
a /models page, last in the header. Header section links underline on
hover only, and every other link keeps the always-underlined default.

pnpm bootstrap asks, masked, for each model API key the configured
provider requires and apps/api/.env leaves empty. Enter skips a key; at
the embedding prompt it reuses the chat key where both reach the same
endpoint, which is the OpenAI default. Off a terminal it asks nothing,
and the closing check still names what is missing.

Closes #5

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
